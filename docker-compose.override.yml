services:
  db:
    env_file:
      - ./.env.dev
  backend:
    env_file:
      - ./.env.dev
    command: python manage.py run
  frontend:
    env_file:
      - ./.env.dev
    ports:
      - "4000:4000"

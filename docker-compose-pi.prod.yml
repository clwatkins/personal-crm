services:
  db:
    env_file:
      - ./.env.prod
  backend:
    image: backend:pi
    command: gunicorn --bind 0.0.0.0:5000 manage:app
    env_file:
      - ./.env.prod
  frontend:
    image: frontend:pi
    env_file:
      - ./.env.prod
    ports:
      - "80:4000"
